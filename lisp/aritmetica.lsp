(setq a 5)
(setq b (+ 5 a))

(setq quadrado (* a a) )

(setq raiz (sqrt 35))

; Tem q aprender como concatenar strings e variaveis.
;(print (concatenate 'list "Quadrado de " #(a) ": " "quadrado" ))
(print b)
(print quadrado)
(print raiz)

